[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "multi-task-plan-depth-study"
version = "1.0.0"
description = "Reproducible CV simulator and results for multi-task plan-depth tuning"
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
authors = [{name = "James Lott"}]
dependencies = []

[tool.setuptools.packages.find]
include = ["known_visit_sim*"]
